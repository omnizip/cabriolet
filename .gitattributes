# Ensure binary files are never modified by Git's line ending conversion
*.cab binary
*.dat binary
*.kwj binary
*.chm binary
*.lit binary

# Test fixtures must remain binary (except Ruby scripts which need proper line endings)
spec/fixtures/** binary
spec/fixtures/**/*.rb text eol=lf